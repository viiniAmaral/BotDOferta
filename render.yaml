services:
  - type: web
    name: ofertabot
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: ofertabot-db
          property: connectionString

databases:
  - name: ofertabot-db
    plan: free
